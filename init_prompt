Сгенерируй мобильное приложение на основе существующе шаблона Android + iOS + Desktop. 
Приложение SpecInspect для ревью спецификаций типа SpecKit, openspec и других.
Проблема которую решает это приложение - сложность ревью спецификаций и одновременного фиксирования неккоректных пунктов, необходимость вникания в весь контекст. Эта проблема решается тем, что пользователь будет ревьюить только по одному сценарию спецификации за раз, помечая что сценарий корректный или нет. Исправление некорректных сценариев будет происходить вне рамках этого приложения, но это приложение позволяет собрать все некорректные сценарии для последующей обработки. 

Описание спецфикации:
Организация спецификации приложения зависит от вида спецификации. Для начала будет реализована только поддержка openspec спецификаций. openspec спецификация состоит из набора supspec находящихся в папке specs. subspec это папка с именем под-спецификации, внутри которой находится файл spec.md.
Файл spec.md описывает подспецификацию.Подспецификация состоит из набора  
Requirement и Scenario. У Requirement краткое описание, подробное описание и набор Scenario. Scnenaro имеет краткое описание, и пункты WHEN и THEN. 
Вот примеры файлов spec.md для демо-спецификации
Пример содержимого spec.md в папке specs/questionnaire-onboarding

## ADDED Requirements

### Requirement: Questionnaire-first onboarding
The system SHALL present an onboarding flow that introduces value-based matching, explains similar and opposite discovery modes, and collects questionnaire answers before discovery is enabled.

#### Scenario: User starts onboarding
- **WHEN** a new user opens onboarding
- **THEN** the app shows the concept summary and begins questionnaire progression with visible progress

### Requirement: Minimum-answer completion gate
The system SHALL block onboarding completion until the user provides at least the configured minimum number of valid answers.

#### Scenario: User attempts to continue with too few answers
- **WHEN** the user taps continue while answer count is below the minimum threshold
- **THEN** the app prevents completion and shows guidance to answer more questions

### Requirement: Mini-profile constraints without avatars
The system SHALL allow profile self-description using only up to 5 words and up to 5 emojis, and SHALL NOT allow avatar upload or avatar display.

#### Scenario: User exceeds profile limits
- **WHEN** the user enters more than 5 words or more than 5 emojis
- **THEN** the app blocks submission and indicates the specific limit violation

Пример содержимого spec.md в папке specs/answer-review-and-confirmation
## ADDED Requirements

### Requirement: Thematic answer review
The system SHALL provide a pre-discovery review screen that summarizes selected answers grouped by themes (relationships, lifestyle, values, communication, future plans).

#### Scenario: User opens answer review
- **WHEN** onboarding answers are completed
- **THEN** the app displays grouped summaries of the user’s selected positions by theme

### Requirement: Editable answers before confirmation
The system SHALL allow users to return to questionnaire editing from the review screen and then return to review with updated summaries.

#### Scenario: User edits an answer from review
- **WHEN** the user taps edit answers and changes at least one response
- **THEN** the review screen reflects updated positions after returning

### Requirement: Review includes mini-profile preview and mode preview
The system SHALL display the user’s 5-word and 5-emoji profile entries and SHALL provide a preview control for both discovery modes before confirmation.

#### Scenario: User previews matching mode options
- **WHEN** the user toggles between similar and opposite previews on review
- **THEN** the app updates the explanatory text to reflect the selected mode behavior

Пример содержимого spec.md в папке specs/dual-mode-match-discovery

## ADDED Requirements

### Requirement: Dual-mode candidate discovery
The system SHALL provide two selectable discovery modes: `similar` for closest-answer candidates and `opposite` for most contrasting-answer candidates.

#### Scenario: User switches discovery mode
- **WHEN** the user changes mode from similar to opposite (or opposite to similar)
- **THEN** the feed refreshes candidate ranking according to the selected mode

### Requirement: Minimal profile card composition
The system SHALL render each candidate card using only 5 words, 5 emojis, compatibility indicator, and concise reason text, without showing photos.

#### Scenario: Candidate card is displayed
- **WHEN** a candidate appears in discovery
- **THEN** the card shows constrained profile fields and rationale text with no avatar or photo elements

### Requirement: Candidate interaction actions
The system SHALL support like/interest, skip, and compatibility-detail actions on each candidate card.

#### Scenario: User likes a candidate
- **WHEN** the user performs like/interest on a card
- **THEN** the system records the reaction and advances the feed to the next candidate

Палитра:

#272838 - темный (основной фон кнопок, текст)
#f3de8a - желтый (акценты, секция THEN)
#eb9486 - коралловый (некорректные карточки, кнопка "влево")
#7e7f9a - серо-фиолетовый (вторичный текст, границы)
#f9f8f8 - светлый фон (основной фон приложения)
#77a0a9 - сине-зеленый (корректные карточки, кнопка "вправо", секция WHEN)

Экраны из которых состоит приложение:
1. Экран онбординга
1.1 Дзиайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=16-50&t=c8bGhbN7OXzdRUWF-0
1.2 Описание экрана
Экран онбординка кратко описывает функциональность приложения, и позволяет перейти к демонстрации работы на демонстрационной спеке, либо перейти к экрану мои спецификации, для начала полноценной работы.

2. Экран Мои спецификации
2.1 Дизайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=12-282&t=c8bGhbN7OXzdRUWF-0
2.2 Описание экрана
Экран мои спецификации показывает список доступных спецификаций, включая демонстрационную спецификацию. Пользователь может удалить спецификацию, и добавить новую спецификацию. Примечание: при удалении демо-спецификации, она просто удаляется с экрана, не удаляя саму демо-спецификацию. Сама демо-спецификация должна оставаться доступной на экране онбординга.

3. Экран просмотра спецификации
3.1 Дизайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=12-334&t=c8bGhbN7OXzdRUWF-0
3.2 Описание экрана
Экран спецификации показывает краткую статистику всех subspec спецификации, и список карточек subspec спецификации. Статистика спецификации показывает секции с числом, сколько subspec корректных, некорректных, и неоцененных. Кнопка Начать ревью позволяет перейти к экрану ревью всех Scenario этой cпецификации. При клике на секцию статистики, лист subspec отфильтровывается соответствуя выбранной секции статистики. Лист карточек subspec отображает subspec спецификации, каждая карточка subspec отображает название subspec, количество Requirement, Scenario и карткую статистику по subspec - сколько корректных, неккоректных и неоцененных Requirement.
При клике на карточку subspec, пользователь навигируется на экран просмотра subspec.

4. Экран просмотра subspec
4.1 Дизайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=12-416&t=c8bGhbN7OXzdRUWF-0
4.2 Описание экрана
Экран subspec показывает краткую статистику всех Requirement subspec, и список карточек Requirement subspec. Статистика subspec показывает секции с числом, сколько Requirement корректных, некорректных, и неоцененных. Кнопка Начать ревью позволяет перейти к экрану ревью всех Scenario этого subspec. При клике на секцию статистики, лист Requirement отфильтровывается соответствуя выбранной секции статистики. Лист карточек Requirement отображает Requirement спецификации, каждая карточка Requirement отображает название Requirement, количество Scenario и карткую статистику по Requirement - сколько корректных, неккоректных и неоцененных Scenario.
При клике на карточку Requirement, пользователь навигируется на экран просмотра Requirement.

5. Экран просмотра Requirement
5.1 Дизайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=12-630&t=c8bGhbN7OXzdRUWF-0
5.2 Описание экрана
Экран Requirement показывает краткую статистику всех Scenario Requirement, и список карточек Scenario Requirement. Статистика Requirement показывает секции с числом, сколько Scenario корректных, некорректных, и неоцененных. Кнопка Начать ревью позволяет перейти к экрану ревью всех Scenario этого Requirement. При клике на секцию статистики, лист Scenario отфильтровывается соответствуя выбранной секции статистики. Лист карточек Scenario отображает WHEN и THEN. Внизу карточки есть переключатель, позволяющий пользователю выбрать корректный это Scenario или некорректный.


6. Экран ревью Scenario
6.1 Дизайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=20-150&t=c8bGhbN7OXzdRUWF-0
6.2 Описание экрана
Экран ревью Scenario позволяет в Tinder манере оценить рандомный непросмотренный Scenario выбранной сущности(spec, subspec, requirement). При свайпе влево, происходит анимация смахивания карточки и Scenario помечается как некорректный. При свайпе вправо, происходит анимация смахивания карточки и Scenario помечается как корректный. Пользователь  может вернуться на экран сущности, с которой он переходил на ревью, через навигацию назад. После просмотра всез непросмотренных Scenario, пользователь навигируется назад на сушность с которой был запущено ревью.

7. Экран добавления спецификации
7.1 Дизайн экрана @Figma https://www.figma.com/design/0bCHsLEe7HLPIeOKoOSvfx/design?node-id=20-104&t=KJgPPehDpilKTb4m-0
7.2 Описание экрана
Экран добавления спецификации позволяет добавть новую спецификацю для отображения на экране спецификаций. Для этого пользователь должен задать ее имя, и выбрать папку с папкой specs